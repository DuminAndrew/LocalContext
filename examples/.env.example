# Sample secret file (fake values) to demonstrate secret detection.
# LocalContext flags this and excludes its contents from the built context.
API_KEY=demo-not-a-real-key-0000000000
DATABASE_URL=postgres://user:password@localhost:5432/demo
