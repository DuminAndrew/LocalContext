// A small helper module for the demo project.
'use strict';

/**
 * Build a friendly greeting.
 * @param {string} name
 * @returns {string}
 */
function greet(name) {
  return `Hello, ${name}!`;
}

module.exports = { greet };
